# The only value the web app needs. Everything else comes from the API's
# bootstrap payload, which is driven by core.Domains and core.PlatformConfig.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
